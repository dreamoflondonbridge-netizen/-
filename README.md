# 👑 다원중 전설의 발표왕 키우기!

중학교 국어 '발표' 단원 학습지 빈칸 복습용 픽셀 아트 육성 시뮬레이션 게임입니다.
`index.html` 파일 하나로 되어 있어서, 브라우저로 열기만 하면 바로 실행됩니다.

## 태블릿에서 바로 열기 (GitHub Pages)

아래 3단계면 학생들에게 링크 하나로 나눠 줄 수 있습니다.

1. 이 저장소의 **Settings** 탭 → 왼쪽 메뉴에서 **Pages** 클릭
2. **Source** 를 `Deploy from a branch` 로 두고,
   **Branch** 를 `claude/presentation-king-game-b5ziim` / `/ (root)` 로 선택 후 **Save**
3. 1~2분 기다리면 아래 주소가 열립니다

   **https://dreamoflondonbridge-netizen.github.io/-/**

> 저장소가 **Private** 이면 Pages 가 유료 플랜에서만 동작합니다.
> 무료 계정이라면 Settings 맨 아래에서 저장소를 **Public** 으로 바꿔 주세요.
> (게임 파일만 들어 있어 공개해도 문제되는 내용은 없습니다.)

주소가 길면 QR 코드로 만들어 칠판에 띄우거나, 클래스룸·패들렛에 링크로 올려 주세요.

## 수업에서 쓸 때

- 소리는 `▶ 발표 육성 시작!` 을 눌러야 켜집니다.
  **아이패드는 측면 무음 스위치가 켜져 있으면 소리가 나지 않습니다.**
- 진행 상황은 **그 기기 안에** 자동 저장됩니다. 앱을 전환하거나 실수로 창을 닫아도
  다시 열면 `▶ 이어서 하기` 가 나타납니다.
  기기를 바꾸거나 브라우저 기록을 지우면 저장 내용도 사라집니다.
- 한 태블릿을 여러 학생이 돌려 쓸 때는 `🔄 처음부터 새로 시작` 을 누르면
  이전 기록이 지워지고 새로 시작합니다.
- 인터넷이 없어도 작동합니다. (픽셀 폰트만 온라인일 때 적용되고, 오프라인이면 기본 글꼴로 대체)

## 문제·엔딩 수정하기

`index.html` 을 메모장이나 VS Code 로 열면 아래 부분을 고칠 수 있습니다.
주석으로 사용법을 적어 두었습니다.

| 고치고 싶은 것 | 찾을 위치 |
|---|---|
| 문제와 정답 | `const SENTENCES = [` |
| 엔딩 캐릭터 이름·설명·이미지 | `const ENDINGS = [` (`imagePath` 값 교체) |
| 점수 배점, 피버 기준 | `GAIN_BASE`, `FEVER1_COMBO`, `FEVER2_COMBO`, `PENALTY` |

## 스테이지별 장소

문제를 풀어 나가면 배경이 자동으로 바뀝니다.

| 스테이지 | 장소 | 내용 |
|---|---|---|
| 1 | 🏫 교실 | 발표 준비 과정 간단 정리 |
| 2 | 🎨 미술실 | 설명 방법 |
| 3 | 🎵 음악실 | 지수의 발표 과정 따라가기 |
| 4 | ⚽ 운동장 | 발표 준비 과정 핵심 정리 |
| 5 | 🍚 급식실 | 발표 내용 체계적으로 조직하기 |
| 6 | 🎪 강당 | 지수의 발표 내용과 매체 자료 |

마지막 문제를 풀면 **커튼이 닫혔다 열리며 강당 무대**가 시작됩니다.
총점에 따라 발표 장면이 4단계로 달라지고, 커튼이 다시 닫히면 엔딩 캐릭터가 나옵니다.

## 엔딩 판정 기준

한 판을 다 풀었을 때 얻을 수 있는 점수는 **최대 1130점** 입니다.
(한 번도 안 틀리고 끝까지 연속 정답을 이었을 때. 한 스탯에 몰아주면 그 스탯이 1130점)

| 순서 | 엔딩 | 조건 |
|---|---|---|
| 1 | 👑 전설의 발표왕 | 4개 스탯 **모두 200점** 이상 |
| 2 | 🎓 다원중 수석 졸업생 | 4개 스탯 **모두 170점** 이상 |
| 3 | 🕵️ 코난 / 🎤 퉁퉁이 / 🦾 토니 / ✨ 장원영 | 해당 스탯 **700점** 이상 |
| 4 | 🐰 주디 / 📢 봉미선 / 🕸️ 피터 / 💖 하츄핑 | 해당 스탯 **400점** 이상 |
| 5 | 📘 발표의 정석 | 4개 스탯 모두 150점 이상 |
| 6 | 🌱 발표 꿈나무 | 4개 스탯 모두 100점 이상 |
| 7 | 🏃 교무실로. | 그 외 |

위에서부터 순서대로 검사해서 **먼저 맞는 조건**이 채택됩니다.
숫자를 바꾸고 싶으면 `index.html` 의 `ENDINGS` 배열에서 `cond:` 부분을 고치면 됩니다.

## 엔딩 이미지 넣기

엔딩마다 그림이 **두 장**씩 들어갑니다. `index.html` 의 `ENDINGS` 배열에서 경로만 바꾸면 됩니다.
이미지 파일이 없어도 그대로 작동하므로(엔딩 카드는 이모지, 무대는 도트 캐릭터로 대체)
천천히 하나씩 채워 넣으셔도 됩니다.

```js
{ name:'다원중 코난', emoji:'🕵️',
  imagePath:'conan.png',              // ← 결과 화면에 나오는 엔딩 캐릭터
  stageImagePath:'stage_conan.png',   // ← 커튼이 열리고 강당 무대에 서는 캐릭터
```

- 같은 폴더에 두면 `"conan.png"`
- `images` 폴더에 두면 `"images/conan.png"`
- 인터넷 주소도 가능 `"https://.../conan.png"`

무대 이미지는 **세로로 긴 그림(대략 3:4 비율)** 이 잘 맞습니다.
배경이 비치도록 **투명 배경 PNG** 를 쓰면 무대 위에 자연스럽게 서 있는 것처럼 보입니다.
넣은 그림은 **도트로 변환하지 않고 원본 화질 그대로** 표시됩니다.

### 인터넷 주소(URL)보다 파일로 넣는 편이 안전합니다

다른 사이트의 이미지 주소를 그대로 붙여 넣으면 아래 문제가 생길 수 있습니다.

- 사이트에 따라 **다른 곳에서 불러오는 것을 막아** 그림이 안 뜰 수 있습니다 (특히 나무위키)
- 구글 이미지 검색 썸네일 주소(`encrypted-tbn0.gstatic.com`)는 **시간이 지나면 사라집니다**
- 유료 사진 사이트 미리보기 주소는 **워터마크가 찍혀** 나옵니다
- 학교 인터넷에서 **차단된 사이트**면 안 보입니다
- 인터넷이 끊기면 안 보입니다

**권장 방법** : 그림을 내려받아 저장소에 `images` 폴더를 만들어 올리고,
`imagePath:'images/conan.png'` 처럼 적으면 게임과 같은 곳에서 불러오므로 항상 안정적으로 뜹니다.

> 이미지가 안 떠도 게임은 멈추지 않습니다.
> 엔딩 카드는 이모지로, 무대는 도트 캐릭터로 자동 대체됩니다.
